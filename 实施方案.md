# ScreenShare 屏幕分享软件 · 实施方案

> 2026-08-04 ｜ 需求确认：5–20 人观看 · 网页 + 客户端 · 延迟 <1s · 月预算几百到上千元

## 一、方案结论

**采用方案 B：SFU 云服务器（LiveKit 自托管）**。服务器只做转发不转码 → 画质无损、延迟 <1s、无需 GPU。

推荐理由：
- LiveKit 是开源 SFU，自带信令、房间管理、鉴权(JWT)、服务端录制，一条龙
- 官方 SDK 覆盖网页(livekit-client) + 桌面/原生，正好满足"两者都要"
- Simulcast 多档码率自适应，20 人同时看也不会卡

## 二、带宽与成本测算

| 画质档 | 码率 | 说明 |
|---|---|---|
| 4K60 · H.265 | 15–25 Mbps | 少数高带宽观众 |
| 1080p60 · H.265 | 4–8 Mbps | 大多数观众 |
| 720p60 · H.265 | 2–3 Mbps | 弱网自动降档 |

20 人峰值规划：2×4K + 8×1080p + 10×720p ≈ **115 Mbps 峰值**
实际流量：平均利用率约 40% ≈ 46 Mbps ≈ 20 GB/小时；每周用 6 小时 ≈ 120 GB/月

| 成本项 | 价格 | 说明 |
|---|---|---|
| 轻量服务器 2核4G · 100Mbps 峰值 | ¥160–300/月 | 含 200GB 流量包 |
| 超出流量 | ¥0.8/GB | 正常使用基本不超 |
| 域名 | ¥50–80/年 | 也可用 sslip.io 免费替代 |
| **合计** | **¥200–500/月** | 符合预算 |

## 三、服务器配置（购买清单）

- 厂商：腾讯云 / 阿里云 轻量应用服务器（Lighthouse）
- 配置：2核4G · 100Mbps 峰值带宽 · 200GB 流量包/月 · 系统盘 50GB
- 地域：
  - **大陆机房**（朋友都在国内，延迟最低）→ 域名需 ICP 备案（1–3 周，个人备案线上可自助完成）
  - **香港机房**（免备案、即开即用，延迟 30–80ms，峰值带宽较小，适合 5–10 人）
- 安全组/防火墙放行：TCP 80/443、TCP 7880、**UDP 50000–60000**（WebRTC 媒体端口，必放！）

## 四、技术栈清单

| 层 | 组件 | 说明 |
|---|---|---|
| 发送端（MVP） | OBS Studio 30+ | WHIP 推流，NVENC / VideoToolbox 硬编 H.265 |
| 发送端（产品化） | 自研采集器 | Windows: WGC + NVENC；macOS: ScreenCaptureKit + VideoToolbox |
| 服务器 | livekit-server（Docker） | SFU + 信令 + 房间 + 鉴权 |
| TLS / 反向代理 | Caddy 2 | 自动 HTTPS（WebRTC 强制要求安全上下文） |
| 网页接收端 | livekit-client + React/Vue | 打开链接即看，无需安装 |
| 桌面接收端 | Electron + livekit-client | 或基于 LiveKit 开源桌面端二开 |
| 录制 | LiveKit Egress | 服务端录制 MP4 到本地盘 |
| 域名 | 备案域名 / sslip.io | HTTPS 必须 |

## 五、分阶段实施计划

### M0 准备（第 1 天）
- 购买轻量服务器 + 域名，提交 ICP 备案（大陆方案）
- 备案期间可用香港机先跑通联调

### M1 部署（1–2 天）
1. Docker 安装 livekit-server，配置信令端口 7880 + WebRTC UDP 端口段
2. Caddy 配置自动 HTTPS + 反向代理到 livekit-server
3. 防火墙/安全组放行端口，写一个生成访问令牌的测试脚本
- ✅ 验收：浏览器打开房间页不报安全错误，能建立连接

### M2 验证（1–2 天）
1. OBS 30+ 添加 WHIP 输出（Custom → WHIP endpoint → 服务器地址）
2. 用 4K60 H.265 推流，网页端播放
3. 验证：清晰度、端到端延迟（<1s）、多档自适应切换、5 人同时观看不卡
- ✅ 验收：延迟 <1s，4K 画面无肉眼可见压缩损失

### M3 网页产品化（1–2 周）
- 房间页：输入邀请码/链接进入，展示"分享中"状态
- Token 鉴权（JWT，控制谁能看）
- Simulcast 档位调优（4K / 1080p / 720p 三档切换阈值）

### M4 客户端 + 录制（2–4 周）
- 桌面端（Electron 封装，含推流端和观看端）
- Egress 录制 + 回放列表页

### M5 进阶（可选）
- AV1 编码（比 H.265 再省 30–50% 码率）
- 多人同时分享、聊天、屏幕 + 摄像头同传

## 六、风险与注意事项

1. **备案**（大陆方案最大时间成本）：1–3 周，个人备案线上自助可完成；赶时间可先买香港机联调
2. **UDP 端口**：安全组/防火墙忘记放行，是 WebRTC 连不上的头号原因
3. **编码端硬件**：4K60 硬编需要近 5 年内的 GPU（NVENC）或 Apple Silicon；老电脑只能 1080p
4. **峰值带宽**：轻量服务器 100Mbps 是峰值，长时间跑满可能被限速
5. **合规**：给朋友用没有问题；若变成公开大规模直播，需按平台/广电要求考虑资质
6. **Egress 录制**：注意磁盘空间，50GB 约可存 30+ 小时 1080p

## 七、参考项目

- LiveKit Server: https://github.com/livekit/livekit
- livekit-client (JS SDK): https://github.com/livekit/client-sdk-js
- OBS Studio: https://obsproject.com（30+ 原生支持 WHIP 推流）
- Caddy: https://caddyserver.com
- sslip.io（免费动态域名，配合 Let's Encrypt 自动签发证书）: https://sslip.io
