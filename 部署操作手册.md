# ScreenShare 部署操作手册（傻瓜版）

> 全程只需要你在腾讯云控制台点几下 + 粘贴 2 段命令。**跟着做，不用懂原理。**
> 做完把终端的输出复制给我，我来帮你检查。

---

## 准备：找到你的服务器

1. 打开腾讯云控制台：https://console.cloud.tencent.com/lighthouse
2. 在服务器列表里找到 **43.161.237.166** 那台，点 **「登录」**

> 会打开一个**黑色背景的网页终端窗口**（像黑客电影里那种）。接下来都在这个窗口里操作。

---

## 第 1 步：进入根目录（粘贴这一行，回车）

```
cd /root
```

## 第 2 步：上传部署包

1. 看网页终端的**顶部/左侧工具栏**，找到 **「上传文件」**（一个向上箭头的图标）
2. 点它，在文件选择框里找到本机这个文件：
   **桌面 → PythonProject → ScreenShare → `screenshare-deploy.tar.gz`**
3. 上传完成（一般几秒钟，18KB 很小）

> 如果找不到"上传"按钮：直接把下面整段内容粘贴进终端，回车，会自动把文件内容重建出来（备用方案，不推荐但可行）。

## 第 3 步：解压并启动部署（粘贴这一行，回车）

```
tar xzf screenshare-deploy.tar.gz && cd screenshare-deploy && bash setup.sh 43.161.237.166
```

然后**等 3~5 分钟**，期间会自动：安装 Docker → 拉取 LiveKit/Caddy/Node 镜像 → 生成配置 → 启动服务。
看到类似下面字样就成功了：

```
================ 部署完成 ================
API 地址  : https://screenshare.43.161.237.166.sslip.io/api
```

**把整个终端输出复制给我**（Ctrl+Shift+C 复制，或者右键全选复制）。

---

## 第 4 步：放行防火墙端口（必做！否则连不上）

1. 回到服务器列表，点进 **43.161.237.166** 这台 → 左侧菜单点 **「防火墙」**
2. 点 **「添加规则」**，按下面加 5 条（协议/端口/来源都照填）：

| 协议 | 端口 | 来源 |
|---|---|---|
| TCP | 80 | 所有 IPv4 |
| TCP | 443 | 所有 IPv4 |
| TCP | 7880 | 所有 IPv4 |
| TCP | 7881 | 所有 IPv4 |
| **UDP** | **50000-50200** | 所有 IPv4 |

> 最后一条是**视频流通道**，漏了它画面永远出不来。端口范围用中间的短横线 `-` 连接。

## 第 5 步：验证（打开浏览器地址栏输入）

```
https://screenshare.43.161.237.166.sslip.io/healthz
```

看到 `{"ok":true}` 就是部署成功 ✅

---

## 遇到问题怎么办

- **命令粘贴后没反应 / 报错** → 把终端里红字内容复制给我，我来判断
- **上传按钮找不到** → 告诉我，我给你备用的粘贴方案
- **第 4 步防火墙在哪** → 打开 https://console.cloud.tencent.com/lighthouse → 点服务器 → 左侧「防火墙」

**任何一步卡住，把终端截图或复制文字发给我就行，我来带你解决。**
